query FetchBlogEntries($pageSize: Int!, $page: Int!) {
  entries(filters: { type: { eq: "blog" } }, pagination: { pageSize: $pageSize, page: $page }) {
    data {
      attributes {
        ...Entry
      }
    }
    meta {
      pagination {
        ...Pagination
      }
    }
  }
}
