query GetPageBySlug($slug: String) {
  pages(filters: { slug: { eq: $slug } }) {
    data {
      attributes {
        title
        content
        slug
        createdAt
        publishedAt
        updatedAt
      }
    }
  }
}
